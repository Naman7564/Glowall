# Temporary nginx config for initial SSL certificate setup
# Use this BEFORE you have SSL certificates

server {
    listen 80;
    server_name glowall.in www.glowall.in;

    # Certbot challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    resolver 127.0.0.11 valid=30s ipv6=off;
    set $upstream http://web:3000;

    location /static/ {
        alias /app/static_root/;
    }

    location /media/ {
        alias /app/media/;
    }

    location / {
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }

    client_max_body_size 20M;
}

server {
    listen 80;
    server_name 80.225.237.255 localhost 127.0.0.1;

    resolver 127.0.0.11 valid=30s ipv6=off;
    set $upstream http://web:3000;

    location /static/ {
        alias /app/static_root/;
    }

    location /media/ {
        alias /app/media/;
    }

    location / {
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }

    client_max_body_size 20M;
}
